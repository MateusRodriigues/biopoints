// lib/utils/app_colors.dart
import 'package:flutter/material.dart';

// Cores Primárias e de Destaque
const Color kPrimaryBlue = Color(0xFF0D47A1); // Azul Escuro Principal
const Color kAccentBlue = Color(0xFF1976D2); // Azul Médio para acentos
const Color kLightBlue =
    Color(0xFFBBDEFB); // Azul Claro para fundos ou destaques suaves

// Cores Neutras
const Color kWhite = Colors.white;
const Color kBlack = Colors.black;
const Color kDarkGrey = Color(0xFF424242); // Cinza Escuro para texto principal
const Color kMediumGrey =
    Color(0xFF757575); // Cinza Médio para texto secundário/ícones
const Color kLightGrey =
    Color(0xFFE0E0E0); // Cinza Claro para divisores, fundos
const Color kVeryLightGrey =
    Color(0xFFF5F5F5); // Cinza Muito Claro para fundos de tela

// Cores de Feedback e Status
const Color kSuccessGreen = Color(0xFF388E3C); // Verde para sucesso
const Color kErrorRed = Color(0xFFD32F2F); // Vermelho para erros
const Color kWarningOrange = Color(0xFFFFA000); // Laranja para avisos

// Novas cores adicionadas para o histórico
const Color kLightGreen = Color(0xFFC8E6C9); // Verde claro para ganhos
const Color kLightRed = Color(0xFFFFCDD2); // Vermelho claro para resgates

// Outras Cores (Exemplos, adicione conforme necessário)
const Color kGold = Color(0xFFFFD700);
const Color kSilver = Color(0xFFC0C0C0);
const Color kBronze = Color(0xFFCD7F32);

// Cores de Texto Específicas
const Color kTextPrimary = kDarkGrey;
const Color kTextSecondary = kMediumGrey;
const Color kTextOnPrimary = kWhite; // Texto sobre a cor primária (ex: botões)
